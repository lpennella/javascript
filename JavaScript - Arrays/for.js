const numeros = [100, 200, 300, 400, 500, 600];

// Primeira expressão: É executada uma única vez.
//Segunda empressão: Condição de execução.
//Terceira empressão: É executada sempre ao final do código.

for (let indice = 0; indice < numeros.length; indice++) {
    console.log(numeros[indice]);
}